# Point this at your backend API (see src/services/api.js for the endpoints it expects).
# Without a backend running, the marketing pages work fully; auth, dashboards and live
# order tracking will show errors since they call this API.
VITE_API_URL=http://localhost:5000/api
